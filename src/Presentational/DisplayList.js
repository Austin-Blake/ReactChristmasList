import React from 'react';
import NewItem from '../Presentational/NewItem';
import ItemList from '../Presentational/ItemList';

export default function DisplayList(newItem, handleChangeName, handleSubmit, handleDeleteItem, allItems, newName) {
    
    

    return (
            <main >
                <h1>Items</h1>
                
                
            
            </main>   
        );   
    };
